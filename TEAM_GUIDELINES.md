# 团队开发规范

适用于当前项目中的 API、应用服务、领域服务、测试与基础设施代码。

## 基本要求

1. **API 返回规范**
   - 所有 API 返回结果统一使用 `Result<T>` 包装。
   - 禁止直接返回裸对象、字符串或不统一的响应结构。

2. **XML 注释规范**
   - 所有 `public` 方法必须编写 XML 注释。
   - 注释至少应包含方法用途、参数说明和返回值说明；必要时补充异常说明。

3. **单元测试规范**
   - 单元测试框架统一使用 **xUnit**。
   - 新增测试或维护现有测试时，不引入其他单元测试框架。

4. **异常处理规范**
   - 禁止直接抛出 `Exception`。
   - 业务场景统一使用明确的业务异常类型，确保语义清晰、便于定位和处理。

5. **日志规范**
   - 所有关键日志必须包含 `TraceId`。
   - 记录请求链路、异常和关键业务事件时，应确保 `TraceId` 可用于完整追踪。

## 执行要求

- 代码评审时以上规范为必检项。
- 新增代码必须遵循本规范，存量代码在修改时逐步对齐。
